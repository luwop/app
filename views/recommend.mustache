<!DOCTYPE html>
<html>
<head>
    <title>Recommended Deck for {{card_name}}</title>
    <link rel="stylesheet" href="/table.css" />
</head>

<body>

<h1 style="text-align:center;">Recommended Deck for {{card_name}} ({{card_id}})</h1>

<div style="width:60%; margin:auto;">

    <p style="font-size:15px; background:#eef; padding:10px; border-left:4px solid #66f;">
        This deck shows the cards that most often work well together.
        Since your selected card is not included, simply <b>swap one card</b>
        in the deck below with <b>{{card_name}}</b>.
    </p>

    {{#deck1.length}}
    <h3>Recommended Deck</h3>
    <table class="CSS_Table_Example">
        <tr><th>Card</th></tr>
        {{#deck1}}
        <tr><td>{{.}}</td></tr>
        {{/deck1}}
    </table>
    {{/deck1.length}}

    {{^deck1.length}}
    <p>No recommended deck data available.</p>
    {{/deck1.length}}

</div>

</body>
</html>
